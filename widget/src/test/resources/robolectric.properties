# Library modules have no targetSdk in their test manifest, so Robolectric would default to the newest
# SDK it ships, ahead of the app's targetSdk (gradle/libs.versions.toml). Pin to the app's target
# instead (docs/ARCHITECTURE.md §6 "Testing strategy").
sdk=36
