# AGENTS.md

The instructions for every agent working in this repository live in [CLAUDE.md](CLAUDE.md) — read it in
full before doing anything, then the docs it points to. This file exists only so tools that look for
`AGENTS.md` find their way there; it deliberately repeats nothing, so the two cannot drift apart.
